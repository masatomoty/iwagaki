# TODO

**生きているタスクだけを置く。** 終わったものの記録は結果側の文書にある
（解析: `docs/RESULTS.md` / 配信性能: `docs/WEB_RESULTS.md` / プラットフォーム: `docs/PLATFORM.md`）。

根拠区分は `docs/DATA.md` と同じ（**[実測]** / **[既知]** / **[仮説]** / **[未確認]**）。

---

## 高

| # | 内容 | 根拠 |
|---|---|---|
| 1 | **初期チャンクを削る** | 実配信の主要チャンクは **521 kB (br)** で、うち maplibre-gl が単体で 232 kB。`slow-highrtt` の 12 秒で運べた 0.91 MB のうち **585 kB が shell** で、地理データは 319 kB しかない。**細い回線で効くのはここだけ**（`docs/WEB_RESULTS.md`）。MapLibre はベースマップを 1 枚も持たず、カメラ・投影・背景色のためだけに載っている |
| 2 | **`threejs-migration` ブランチのマージ判断** | 上の対応として別セッションが three.js 移行を実装済み（未マージ）。初期 JS 425,942 → 124,337 B（−71 %）、正射投影も実装。**マージすると描画層が全面的に入れ替わる**ので、`docs/WEB_DESIGN.md` の書き直しと `docs/WEB_RESULTS.md` の再測が伴う |
| 3 | **点群 LOD の視錐台判定を厳密にする** | いまは可視 bbox の外接矩形で絞っている。傾けた視野は台形なので広めに残る。落としすぎるより安全な側だが、`slow-highrtt` では取りすぎが効く可能性がある |

---

## 中

| # | 内容 | 根拠 |
|---|---|---|
| 4 | **`objects.geojson` のパースコスト** | 転送は wire 109 kB で**バイトの問題ではない**。残る理由は **662 kB の JSON を一括 `JSON.parse` する CPU コスト**。ストリーミング化か PMTiles 化 |
| 5 | **LAS アップロード経路**（Worker + D1 + R2 multipart） | 実点群が viewer で扱えることは確認済み。パートサイズ・ジョブ分割・COPC 生成パラメータを実測値基準で決められる。**315 MB 超の R2 アップロード経路は確立済み**（`web/deploy/r2put.sh`） |
| 6 | **`slow-highrtt` 向けに LOD を浅く止める閾値** | 12 秒で細メッシュに届かない。**ただし優先度制御と同じ罠に注意**: 1 Mbps では点群も PLATEAU も 12 秒の窓で 1 リクエストも出ていない。**初期チャンクを削って帯域が空いてから**測り直す |
| 7 | **高潮浸水想定区域図との突き合わせ** | **舞鶴は高潮浸水想定区域に指定されている**（`docs/DATA.md`）。公表は PDF 7 本 / 計 19 面、すべてラスタで GIS データの配布は無い。まず吉原を含む面を特定する。**京都府側は堤防決壊・越波・河川流量を含む動的計算**なので、静水位モデルのこちらと数値の一致を期待する比較にはならない。位置づけの確認に留めるのが妥当 |
| 8 | **設計高潮位 / 計画高潮位の個別海岸の数値** [未確認] | 丹後沿岸海岸保全基本計画に算定式（`計画天端高 = 計画高潮位 + うちあげ高 + 余裕高`）と「防護水準は過去に記録されている高潮に基づく計画高潮位」という定義はあるが、**舞鶴の個別海岸の数値そのものが無い**。定義上は既往最高潮位 T.P.+0.93 m が基礎のはずだが、同一視する根拠が文書に無いので **[仮説] 止まり**。推測で埋めない |
| 9 | **水門・樋門・陸閘の位置と敷高** [未確認] | **連結性モデルの最大の弱点**。seed / 障壁として反映できれば `h_conn` の前提が変わる。入手できなければ「考慮していない」と明記し続ける |

---

## 低

| # | 内容 | 根拠 |
|---|---|---|
| 10 | **Draco / デバッグ用の CDN 参照をバンドルから消す** | 現行ビルドでは発火しない（22 個の b3dm に `KHR_draco_mesh_compression` が無く、実配信でクロスオリジンのリクエストは 0 件）。ただし `www.gstatic.com/draco/…`・`unpkg.com/webgl-debug`・`cdn.jsdelivr.net/npm/spectorjs` の URL は残る。**潜在リスク**なので、ローカル同梱に固定し「クロスオリジンを 1 件も出さない」を `deploy/check.mjs` の MUST に入れる |
| 11 | **custom point-cloud renderer** | `docs/WEB_DESIGN.md`「レンダラを差し替える条件」の移行条件に届いていない。常駐 12,174 点 / draw call 1 で、置き換える理由が無い |
| 12 | **タイル URL のバージョニング** | `data/tiles` と `data/3dtiles` は `immutable` だが URL に内容ハッシュが無い。**データを更新する運用に入るとここが最初に効く**（`docs/INFRA.md`） |

---

## やると決めていないもの（判断が要る）

- **判定が変わる地物の一覧表示 / 書き出し。** いまは地図をクリックして 1 件ずつしか見られない。
  庁内で使うなら一覧や CSV が要りそうだが、`docs/DESIGN.md` の「やらないこと」に
  **「過度な UI 作り込み、印刷レイアウト」**を挙げている。要件を確認してから決める。
- **時系列（潮位の時間変化）。** 同上「やらないこと」。静水位モデルなので、
  出すなら「時間発展ではなくパラメータ掃引」であることを画面で明示する必要がある。
