# TODO

**生きているタスクだけを置く。** 終わったものの記録は結果側の文書にある
（解析: `docs/RESULTS.md` / 配信性能: `docs/WEB_RESULTS.md` / プラットフォーム: `docs/PLATFORM.md`）。

根拠区分は `docs/DATA.md` と同じ（**[実測]** / **[既知]** / **[仮説]** / **[未確認]**）。

---

**いま「高」は空。** 配信性能まわり（初期チャンク・Draco・帯域推定・LOD 予算・視錐台）は
実測で片付いた。次に何を「高」に上げるかは **2026-09-05 の発表で何を言うか**から
決めるべきで、候補は下の 3〜5（入手に時間がかかる外部データ）である。

## 中

| # | 内容 | 根拠 |
|---|---|---|
| 1 | **LAS アップロード経路**（Worker + D1 + R2 multipart） | 実点群が viewer で扱えることは確認済み。パートサイズ・ジョブ分割・COPC 生成パラメータを実測値基準で決められる。**315 MB 超の R2 アップロード経路は確立済み**（`web/deploy/r2put.sh`） |
| 2 | **正射投影での点群 LOD** [未確認] | `sse` の式は透視のカメラ距離を前提にしている。軸方向プリセット（正射）のときの採否を検証していない |
| 3 | **高潮浸水想定区域図との突き合わせ** | **舞鶴は高潮浸水想定区域に指定されている**（`docs/DATA.md`）。公表は PDF 7 本 / 計 19 面、すべてラスタで GIS データの配布は無い。まず吉原を含む面を特定する。**京都府側は堤防決壊・越波・河川流量を含む動的計算**なので、静水位モデルのこちらと数値の一致を期待する比較にはならない。位置づけの確認に留めるのが妥当 |
| 4 | **設計高潮位 / 計画高潮位の個別海岸の数値** [未確認] | 丹後沿岸海岸保全基本計画に算定式（`計画天端高 = 計画高潮位 + うちあげ高 + 余裕高`）と「防護水準は過去に記録されている高潮に基づく計画高潮位」という定義はあるが、**舞鶴の個別海岸の数値そのものが無い**。定義上は既往最高潮位 T.P.+0.93 m が基礎のはずだが、同一視する根拠が文書に無いので **[仮説] 止まり**。推測で埋めない |
| 5 | **水門・樋門・陸閘の位置と敷高** [未確認] | **連結性モデルの最大の弱点**。seed / 障壁として反映できれば `h_conn` の前提が変わる。入手できなければ「考慮していない」と明記し続ける |

---

## 低

| # | 内容 | 根拠 |
|---|---|---|
| 6 | **デバッグ用の CDN 参照をバンドルから消す** | Draco は同一オリジン配信に直した（`docs/WEB_RESULTS.md`）。残る `unpkg.com/webgl-debug`・`cdn.jsdelivr.net/npm/spectorjs` は URL が残るだけで、**`deploy/check.mjs` の「外部オリジンへのリクエストが 0 件」は通っている**。発火条件は未確認 [未確認] |
| 7 | **`Points` の先の custom point-cloud renderer** | `docs/WEB_DESIGN.md`「レンダラを差し替える条件」に届いていない。deck.gl `PointCloudLayer` からの差し替えは済んでおり、いま `Points` で困っている兆候は無い |
| 8 | **タイル URL のバージョニング** | `data/tiles` と `data/3dtiles` は `immutable` だが URL に内容ハッシュが無い。**データを更新する運用に入るとここが最初に効く**（`docs/INFRA.md`） |

---

## やると決めていないもの（判断が要る）

- **判定が変わる地物の一覧表示 / 書き出し。** いまは地図をクリックして 1 件ずつしか見られない。
  庁内で使うなら一覧や CSV が要りそうだが、`docs/DESIGN.md` の「やらないこと」に
  **「過度な UI 作り込み、印刷レイアウト」**を挙げている。要件を確認してから決める。
- **時系列（潮位の時間変化）。** 同上「やらないこと」。静水位モデルなので、
  出すなら「時間発展ではなくパラメータ掃引」であることを画面で明示する必要がある。
- **`fast4g` で建物 2 枚と点 56,000 点のどちらを採るか。** 点群の予算を
  1 MB から 0.5 MB に下げると 12 秒窓の建物が 20/22 -> 22/22 になるが、
  常駐点数が 68,622 -> 12,612 に落ちる（`docs/WEB_RESULTS.md`）。
  いまは点群側を採っている（残り 2 枚は 12 秒直後に届く）。**用途で決まる話**なので、
  庁内での使われ方を確認してから決める。
