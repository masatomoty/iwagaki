# TODO

**生きているタスクだけを置く。** 終わったものの記録は結果側の文書にある
（解析: `docs/results.md` / 配信性能: `docs/web_results.md` / プラットフォーム: `docs/platform.md`）。

根拠区分は `docs/data.md` と同じ（**[実測]** / **[既知]** / **[仮説]** / **[未確認]**）。

---

**いま「高」は空。** 標高成果の換算（測地成果2024 → 2011）と、実測との食い違いの
切り分けは片付いた。後者は「排水されている」のではなく**この水位帯で判定が 1〜2 cm で
倍変わる**ためだった（`docs/results.md`「なぜモデルの方が多く出るのか」）。
残る (a) 実測が下限であること と (c)「浸水位」の定義は**市への照会（中 4）**に寄せた。

**配信性能まわりも片付いた**（初期チャンク・Draco・帯域推定・LOD 予算・視錐台）。
中のうち **3 と 4 は市に照会済みで返答待ち**である。

**市（建設部 都市計画課）から viewer への要望が 4 点来た**（2026-08、`高潮表示範囲.pdf`）。
**4 点すべて入れた**（画面側は `docs/web_design.md`、範囲拡大は `docs/usage.md`）。

| 要望 | 状態 |
|---|---|
| 海面は潮位を変えると高さも変動するのか | **入れた。** それまで水面という物体が無く、地形の画素を塗るだけだったので高さは動いていなかった。潮位の高さに水平な水面メッシュを張る（配信物は増えない）。**配信後に「海面なくね」と再指摘があり、港と湾が一枚も描かれていないことが分かった** — 航空レーザは水面から反射が返らないので平常時の水域の大半が DEM の nodata で、シェーダが両パスとも捨てていた。水面は標高を知らなくても張れるので、nodata でも「水深不明の水域」として張るようにした（`docs/web_design.md`「海が一枚も描かれていなかった」） |
| 浸水深 50cm で床下 / 床上に建物を塗り分ける | **入れた。** `buildingColor: 'depth'`。既往最高潮位 0.93 m で床上 32 棟 / 床下 302 棟、高潮想定の基準潮位 0.69 m で**床上 0 棟**（3 次メッシュを直したあとの 930 棟が母数。直す前は 694 棟で 30 / 277 だった）。要望に添えられていた「そこまで上がる場所は少ない」は被害記録（床上 0 戸）とも一致する |
| PLATEAU の道路を分かるように出す | **入れた（3 回作り直した）。** 道路（吉原 293 本）は前から描いていたが建物と同じ配色で区別できなかった。既定は**一律のほぼ白 ＋ ほぼ黒の縁**、通行支障は `道路の色` の選択式に下げた。**色は画面全体で 1 つの予算**で、1 レイヤだけ見て決めると必ずどこかとぶつかる（`docs/web_design.md`「画面の色は 1 つの予算である」）。ついでに**地物が地形に洗われていた描画順の誤り**が見つかった |
| **〇で囲った 2 範囲を 0.5m DEM で可視化**（西舞鶴・東舞鶴、点群は無し） | **入れた。** 各 625 ha を `IWAGAKI_AOI` で選べるようにして焼いた（`docs/usage.md`）。viewer に「対象範囲」を足し、入口を `data/areas.json` にした。**源 ≫ 解像度の順序が 3 範囲すべてで再現した**（`docs/results.md`） |

**その配信のあと、東舞鶴について市から要望が来た**（2026-08-25）。

| 指摘 | 状態 |
|---|---|
| **表示範囲の東側を JR 線路まで広げてほしい**（赤破線の入った地図が添付） | **入れた。** 総括図を GSI 標準地図に海岸線で合わせて赤破線を座標に起こし（残差 ±60 m）、**東舞鶴駅から愛宕浜町へ向かう JR 小浜線の上に乗る**ことを確認した。破線の東端は E = −54020 で旧東端の 480 m 外。東端は破線ではなく**低地が尽きる位置**（GSI 5m DEM で標高 3 m 以下の割合が E = −53100 でほぼ 0 になる）で決めて **−53000**。625 ha → **1,000 ha**（4.0 × 2.5 km）。地物 3,708 → **8,368**、タイル 620 → 960 枚。**FMR を待つ粗タイルは 6 枚のままで変わらない**（`docs/web_results.md`）。破線の南西端だけは南端の 172 m 外に残るが、依頼が「東側」なので南は動かしていない |

**副産物として吉原の地物が 34 % 増えた。** 建物・道路を 3 次メッシュ 1 枚からしか
取っていなかったのを 4 枚に直した（911 → 1,223 地物）。取りこぼしは入江と山手ではなく
**市街**で、既往最高潮位での浸水建物が +9 % 増える。`README.md` / `docs/results.md` /
`docs/web_design.md` / `docs/data.md` の地物の数字は全部差し替えた。
ラスタ側（12.7 % など）は建物・道路に依らないので変わっていない。
経緯は `docs/results.md`「3 次メッシュの取りこぼし」。

**広い範囲の初期表示も直した。** FMR が 2〜4 倍遅かったのは、粗ピラミッドの
上限が z15 固定で **AOI の粗タイルをほぼ全部待っていた**ため。範囲が広いときだけ
1 段粗くして、西舞鶴 3,994 → 1,812 ms / 東舞鶴 4,489 → 1,307 ms
（`docs/web_results.md`「FMR が遅かったのは枚数であってバイトではない」）。

## 中

| # | 内容 | 根拠 |
|---|---|---|
| 1 | **高潮浸水想定区域図との突き合わせ** | **舞鶴は高潮浸水想定区域に指定されている**（`docs/data.md`）。公表は PDF 7 本 / 計 19 面、すべてラスタで GIS データの配布は無い。まず吉原を含む面を特定する。**京都府側は堤防決壊・越波・河川流量を含む動的計算**なので、静水位モデルのこちらと数値の一致を期待する比較にはならない。位置づけの確認に留めるのが妥当 |
| 2 | **設計高潮位 / 計画高潮位の個別海岸の数値** [未確認] | 丹後沿岸海岸保全基本計画に算定式（`計画天端高 = 計画高潮位 + うちあげ高 + 余裕高`）と「防護水準は過去に記録されている高潮に基づく計画高潮位」という定義はあるが、**舞鶴の個別海岸の数値そのものが無い**。定義上は既往最高潮位 T.P.+0.93 m が基礎のはずだが、同一視する根拠が文書に無いので **[仮説] 止まり**。推測で埋めない |
| 3 | **排水路の吐口の位置と敷高** [未確認] | **水門・樋門は入江に無いことが確認できた**（市への照会、2026-08）。連結性モデルの「最大の弱点」は閉鎖構造ではなく**管路**に移った。吐口は**現在把握されておらず市が来年度に調査委託を出す予定**で、**フラップゲートが無い**ので潮位が上がれば逆流しうる。地表面の連結性しか見ていない本モデルは**内陸側を過小評価している**。 |
| 4 | **被害調査の位置データと「浸水位」の定義を市に依頼する** | **資料は 2 回分受領済み**（2026-08。`docs/results.md`「実際の被害記録との突き合わせ」）。潮位 0.68 / 0.67 m で床下 39 / 19 戸、**1 cm 差で倍違う**という `h_conn` が最も効く領域のデータだった。ただし**地物単位の突き合わせが止まっている** — 資料の図面は Google Maps のスクリーンショットで座標が無く、PLATEAU 側にも住所属性が無い（`bldg:address` が 0 件）。依頼するのは (1) 図面の青塗り部分の位置データ (2) **「浸水位」の定義**（地盤面からの水深か床面からの高さか。モデルの浸水深が実測より深く出ている件の解釈が変わる） |

---

## 低

| # | 内容 | 根拠 |
|---|---|---|
| 5 | **`Points` の先の custom point-cloud renderer** | `docs/web_design.md`「レンダラを差し替える条件」に届いていない。deck.gl `PointCloudLayer` からの差し替えは済んでおり、いま `Points` で困っている兆候は無い |

---

## やると決めていないもの（判断が要る）

- **判定が変わる地物の一覧表示 / 書き出し。** いまは地図をクリックして 1 件ずつしか見られない。
  庁内で使うなら一覧や CSV が要りそうだが、`docs/design.md` の「やらないこと」に
  **「過度な UI 作り込み、印刷レイアウト」**を挙げている。要件を確認してから決める。
- **時系列（潮位の時間変化）。** 同上「やらないこと」。静水位モデルなので、
  出すなら「時間発展ではなくパラメータ掃引」であることを画面で明示する必要がある。
- **「0.5m とは何のデータか」を画面に戻すか。** 説明文を全部外したあと、
  外部から**まさにその質問**が来た（2026-08）。いまは select のツールチップだけ。
  凡例あたりに一行だけ戻す案がある。
- **点群を出しているとき PLATEAU 建物をどう扱うか。** 箱が点群を覆うので、
  両方 ON で寄ると「建物の形が見えない」。半透明 / 稜線だけ / 自動 OFF / 何もしない。
- **`diff_res`（解像度だけ）を `判定差` ボタンに出すか。** 焼いてあるが載せていない。
  `highres` に割り当てると「0.5m の判定差」の意味が合計 12.7 % から解像度だけの
  3.4 % に変わり、`README.md` と `docs/results.md` の見出しと食い違う。
  **発表でどちらを主役にするか**で決まる。
- **点群のノイズ点。** SLAM は空に散る点を残していて、実測 RGB で塗ると
  水色の粒として目立つ（彩度を 1.15 に抑えて緩和はしてある）。
  高さでクリップするか、配信前の間引きで落とすかは**データ側の判断**。
- **`fast4g` で建物 2 枚と点 56,000 点のどちらを採るか。** 点群の予算を
  1 MB から 0.5 MB に下げると 12 秒窓の建物が 20/22 -> 22/22 になるが、
  常駐点数が 68,622 -> 12,612 に落ちる（`docs/web_results.md`）。
  いまは点群側を採っている（残り 2 枚は 12 秒直後に届く）。**用途で決まる話**なので、
  庁内での使われ方を確認してから決める。
