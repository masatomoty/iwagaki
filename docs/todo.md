# TODO

**生きているタスクだけを置く。** 終わったものの記録は結果側の文書にある
（解析: `docs/results.md` / 配信性能: `docs/web_results.md` / プラットフォーム: `docs/platform.md`）。

根拠区分は `docs/data.md` と同じ（**[実測]** / **[既知]** / **[仮説]** / **[未確認]**）。

---

**いま「高」は空。** 標高成果の換算（測地成果2024 → 2011）と、実測との食い違いの
切り分けは片付いた。後者は「排水されている」のではなく**この水位帯で判定が 1〜2 cm で
倍変わる**ためだった（`docs/results.md`「なぜモデルの方が多く出るのか」）。
残る (a) 実測が下限であること と (c)「浸水位」の定義は**市への照会（中 4）**に寄せた。

**配信性能まわりも片付いた**（初期チャンク・Draco・帯域推定・LOD 予算・視錐台）。
中のうち **3 と 4 は市に照会済みで返答待ち**である。

**市（建設部 都市計画課）から viewer への要望が 4 点来た**（2026-08、`高潮表示範囲.pdf`）。
**4 点すべて入れた**（画面側は `docs/web_design.md`、範囲拡大は `docs/usage.md`）。

| 要望 | 状態 |
|---|---|
| 海面は潮位を変えると高さも変動するのか | **入れた。** それまで水面という物体が無く、地形の画素を塗るだけだったので高さは動いていなかった。潮位の高さに水平な水面メッシュを張る（配信物は増えない）。**配信後に「海面なくね」と再指摘があり、港と湾が一枚も描かれていないことが分かった** — 航空レーザは水面から反射が返らないので平常時の水域の大半が DEM の nodata で、シェーダが両パスとも捨てていた。水面は標高を知らなくても張れるので、nodata でも「水深不明の水域」として張るようにした（`docs/web_design.md`「海が一枚も描かれていなかった」） |
| 浸水深 50cm で床下 / 床上に建物を塗り分ける | **入れた。** `buildingColor: 'depth'`。既往最高潮位 0.93 m で床上 32 棟 / 床下 302 棟、高潮想定の基準潮位 0.69 m で**床上 0 棟**（3 次メッシュを直したあとの 930 棟が母数。直す前は 694 棟で 30 / 277 だった）。要望に添えられていた「そこまで上がる場所は少ない」は被害記録（床上 0 戸）とも一致する |
| PLATEAU の道路を分かるように出す | **入れた（3 回作り直した）。** 道路（吉原 293 本）は前から描いていたが建物と同じ配色で区別できなかった。既定は**一律のほぼ白 ＋ ほぼ黒の縁**、通行支障は `道路の色` の選択式に下げた。**色は画面全体で 1 つの予算**で、1 レイヤだけ見て決めると必ずどこかとぶつかる（`docs/web_design.md`「画面の色は 1 つの予算である」）。ついでに**地物が地形に洗われていた描画順の誤り**が見つかった |
| **〇で囲った 2 範囲を 0.5m DEM で可視化**（西舞鶴・東舞鶴、点群は無し） | **入れた。** 各 625 ha を `IWAGAKI_AOI` で選べるようにして焼いた（`docs/usage.md`）。viewer に「対象範囲」を足し、入口を `data/areas.json` にした。**源 ≫ 解像度の順序が 3 範囲すべてで再現した**（`docs/results.md`） |

**その配信のあと、東舞鶴について市から 2 点来た**（2026-08-25）。

| 指摘 | 状態 |
|---|---|
| **0.8 m の潮位でも浸水する範囲があるはずなのに着色されない。** 地盤高 0.8 m のところで潮位を 0.9 m にしても浸水深が 0 のまま | **入れた。コードの誤りではなかった。** 東舞鶴の市街は護岸天端 1.0〜1.3 m に囲まれた 0.8〜1.0 m の窪地で、**潮位を 0.45 → 0.95 m と 0.5 m ぶん上げても市街では 1.37 ha しか増えない**のに次の 0.3 m で 14.23 ha 増える [実測]。連結性モデルがそのとおりに効いている。ただし吐口にフラップゲートが無い以上（中 3）**過小評価はこちらの側**なので、**窪地を別の印で出すようにした**（配信物は増えない。潮位 0.93 m で 10.57 ha / 321 地物、建物 201 棟）。地物パネルも「浸水深 0.00 m」で終わらせず理由を出す。`docs/web_design.md`「窪地は『浸水しない』と同じ色で潰さない」。**副産物として東舞鶴の 0.5m DEM が 0.1 m 刻みであることが分かった**（西舞鶴の海際は 0.001 m 刻み。`docs/data.md`「鉛直の刻みは図郭によって違う」） |
| **表示範囲の東側を JR 線路まで広げてほしい**（赤破線の入った地図が添付） | **入れた。** 総括図を GSI 標準地図に海岸線で合わせて赤破線を座標に起こし（残差 ±60 m）、**東舞鶴駅から愛宕浜町へ向かう JR 小浜線の上に乗る**ことを確認した。破線の東端は E = −54020 で旧東端の 480 m 外。東端は破線ではなく**低地が尽きる位置**（GSI 5m DEM で標高 3 m 以下の割合が E = −53100 でほぼ 0 になる）で決めて **−53000**。625 ha → **1,000 ha**（4.0 × 2.5 km）。地物 3,708 → **8,368**、タイル 620 → 960 枚。**FMR を待つ粗タイルは 6 枚のままで変わらない**（`docs/web_results.md`）。破線の南西端だけは南端の 172 m 外に残るが、依頼が「東側」なので南は動かしていない。<br>**線路そのものも画面に足した**（再指摘、2026-08-25）。範囲を広げても基準線が無いと「どこまで広げたのか」が読めない。**PLATEAU 舞鶴市に鉄道は無い**（`udx/` に `rwy` が無く `tran` は Road だけ）ので国土数値情報 N02 から切り出した（東舞鶴 2.77 km / 西舞鶴 1.27 km / 吉原 0）。**見えなかった原因は色でも太さでもなく描画順**で、Mesh の `renderOrder` では地物の Group（30）に上塗りされていた（マゼンタ画素 104 → 2,301）。色はその後**黒白の鉄道記号に戻し**、さらに**細く弱く**した（幅 7 m・道路と同じ `depthTest`）。「浮いている／紙の地図はこんなにはっきりさせない」の再指摘に対応。`docs/web_design.md`「線路が見えなかったのは色ではなく描画順だった」 |

**仮想排水路（S2）と「仮定の段階」を入れた**（2026-08-26〜27、東舞鶴のみ）。
市の回答「今後は排水路の口の高さやフラップゲートを考慮したシミュレーションが要る」
（2026-08-26）に対して、吐口が無い状態とある状態の間を埋めるモデルを足した
（`docs/flood_simulation_spec.md`）。

| やったこと | 状態 |
|---|---|
| 仮想吐口ペアの生成と S2 の到達水位 | **入れた。** 12 組（`scripts/32` が窪地の大きい順に置く。**実在施設ではない**）。既往最高潮位 0.93 m で連結 16.17 ha → **24.85 ha** |
| **仮定の段階**（仕様 Phase 4） | **入れた。配信物は 1 バイトも増えていない。** 当初の軸（吐口の敷高 ±0.20 m）は**退化していた** — 3 ケースで既往最高潮位の浸水面積が 24.85 ha で完全に一致し、差は 9〜11 セル（0.0003 ha）だけだった [実測]。敷高は −0.30〜+0.30 m T.P. に収まっていて、潮位 0.50 m 以上ではどのケースでも 12 組すべてが seed になる。**S2 の感度を支配しているのは敷高ではなく吐口の有無**。軸をモデルの入れ子（`S1 ⊆ S2 ⊆ 潮位以下`）に替え、3 段はすべて既存の `diff_drainage` タイルの R/G/B から読む。**新たに浸かる 17.5 ha のうち 8.68 ha（半分）が「吐口がある」という仮定に依存する** [実測]。配信済みタイル 693 枚を復号して解析ラスタと一致すること・入れ子の破れが 0 画素であることを確認した |
| 仮想モデルの注意書き（仕様 §1 の必須表示） | **入れた。** 凡例に常時出す。それまで select のツールチップ 1 行しか無かった |
| **次の高潮時に見るべき地物のリスト** | **入れた（2026-08-28）。** 2 段（`h_conn_highres > H` かつ `h_conn_drainage <= H`）だけを CSV / GeoJSON に書き出す。既往最高潮位 0.93 m で 425 地物（東 271 / 西 154）、高潮想定の基準潮位 0.69 m で 5 地物 [実測]。画面は作らずファイルだけ（`docs/results.md`「次の高潮時に見るべき地物のリスト」） |

**2026-09-01 更新: `scripts/32` の吐口配置を「窪地の大きい順・最低セル」から
「面積 1000 m² 以上・越流点標高の低い順・越流点セル」に変えた**
（`docs/flood_simulation_spec.md` §5.2、`docs/results.md`「越流点ベースで『吐口が
あれば』の 2 段が縮む」）。上の数字はその前のもの。新しい S2 到達水位・面積:
東舞鶴 0.93 m で 24.85 → **20.46 ha**（12 組）、西舞鶴 38.73 → **36.73 ha**
（条件を満たす窪地が 9 個で 9 組）。「吐口がある」仮定に依存する 2 段は
東 8.68 → 4.29 ha / 西 2.00 → 実質 0 に縮み、そのぶん経路を示せない 1 段へ移った。
西舞鶴は S2 が S1 とほぼ同一。合計（`地盤高 ≤ H` − 平常時水域）は不変。
`run_all.sh` は **33 → 32 → 31** の順に変更。

**残っているもの。**

- ~~西舞鶴に仮想排水路が無い~~ → **入れた**（2026-08-27）。12 組。既往最高潮位で
  連結 36.73 ha → 38.73 ha。**東舞鶴とは絵が正反対**で、仮定に依存するのは
  2.00 ha だけ（東舞鶴は 8.68 ha）。海と川が市街まで入り込んでいて地表面で
  直接つながるため。**敷高 3 ケースの退化も同じく再現した**（`docs/results.md`）
- **吉原には入れていない。** 4 条件があり主張（地形データの差）を担う範囲なので、
  仮想モデルを混ぜるかは判断が要る
- **吐口の密度そのものを軸にするか。** `--max-pairs` を振ると絵が動くのは
  実測済み（面積上位 12 組で窪地の 83 %。越流点ベースでは `spill_elev` 下位から
  何組取るかで 2 段の面積が変わる）。敷高より効く唯一の軸だが、S2 は既に単純
  モデルに近いので、段の絵がどこまで変わるかは未確認

**副産物として吉原の地物が 34 % 増えた。** 建物・道路を 3 次メッシュ 1 枚からしか
取っていなかったのを 4 枚に直した（911 → 1,223 地物）。取りこぼしは入江と山手ではなく
**市街**で、既往最高潮位での浸水建物が +9 % 増える。`README.md` / `docs/results.md` /
`docs/web_design.md` / `docs/data.md` の地物の数字は全部差し替えた。
ラスタ側（12.7 % など）は建物・道路に依らないので変わっていない。
経緯は `docs/results.md`「3 次メッシュの取りこぼし」。

**広い範囲の初期表示も直した。** FMR が 2〜4 倍遅かったのは、粗ピラミッドの
上限が z15 固定で **AOI の粗タイルをほぼ全部待っていた**ため。範囲が広いときだけ
1 段粗くして、西舞鶴 3,994 → 1,812 ms / 東舞鶴 4,489 → 1,307 ms
（`docs/web_results.md`「FMR が遅かったのは枚数であってバイトではない」）。

**上田氏（舞鶴市）から追加の要望が来た**（2026-08-28、Slack）。

| # | 要望 | 状態 |
|---|---|---|
| ③ | **浸水建物を国勢調査の小地域（町丁・字等）ごとに集計して地域比較できるようにする** | **入れた（viewer 表示も入れた）。** `scripts/13_fetch_census_boundaries.py` が e-Stat 統計GIS の 2020年国勢調査 小地域境界を取り（`docs/data.md` §5）、`scripts/92_area_aggregate.py` が建物の重心で空間結合して潮位別・モデル別に `data/out/{aoi}/area_flood_H*.{csv,geojson}` ＋ `area_flood_summary.json` を出す。合計は `web/src/domain/flood.ts` の `floorCounts` と一致（`docs/results.md`「小地域ごとの浸水建物」）。既往最高潮位 0.93 m で床上は西舞鶴の旧舞鶴東吉原一に集中、東舞鶴は市街の町丁で `simple`↔`connected` が大きく割れる。<br>**viewer には潮位別の値を焼かず、`scripts/83` が `scripts/92` と同一結合で建物ごとに `area_code` を `objects.geojson` へ焼き、潮位再生パネル下の地域別ランキング表がその場で集計する**（`docs/web_design.md`「小地域ごとの棟数はどう出したか」）。**コロプレスは色予算で見送り**、`small_areas.geojson` は `catalog.small_areas` に optional 配信のみ（follow-up は viewer 側だけ）。 |

（同じ 2026-08 のやり取りから派生した「交通規制が必要な道路の可視化」は上の
`scripts/91`、「次の高潮時に見るべき地物のリスト」は `scripts/88`。）

**複数自治体（京都市・さいたま市）から周辺分析の要望が来た**（2026-08-28〜29、Discord。
CITY GAP / plateau-city-gap 側のやり取りだが本リポジトリの `scripts/13`・`scripts/92`・
道路グラフを受け皿にできる）。

| # | 要望 | 状態・進め方 |
|---|---|---|
| T1 | **任意地点を中心にした範囲集計。** 500m メッシュ起点をやめ、地図上の任意点（駅に限らない）を中心に半径 **500 / 800 / 1000m 切替**＋**町丁・字単位**で集計する。表示順は 山本氏の回答どおり ① 人口と年齢分布 ② 建物用途分布 ③ 事業所数 ④ 用途地域等の都市計画制限 ⑤ 交通。800m は国交省『都市構造評価ハンドブック』の徒歩圏、500m は高齢者徒歩圏 **[実測: 要望]** | **未着手。** `scripts/92` の「矩形 AOI → 建物重心で空間結合」を円／小地域ポリゴンに一般化する。③④は別データ取得（経済センサス・用途地域）に切り出し、まず人口・建物・交通で骨格。 |
| T2 | **徒歩圏（道路ネットワーク等時線）。** T1 の中心から道路ネットワーク上で 10 分圏を面で出し、単純バッファと別レイヤーで併置して乖離を見せる **[実測: 要望]** ／ ネットワークの妥当性は **[仮説]** | **未着手。** PLATEAU 道路グラフ（`scripts/91` 系、OSM 照合 Spearman 0.95）を使う。公式歩行者網ではない旨を常時表示。ネットワークがある AOI 内でのみ有効。 |
| T3 | **潮位・測地成果まわりの記録更新**（上田氏の回答, 2026-08-29） **[実測: 市回答]** | **反映済み（`docs/data.md`「市の潮位・浸水記録の扱い」）。** 市内潮位は験潮所の値を一律採用／気象庁値は元から T.P.／今年度から測地成果2024（市資料は旧成果、変換は地点ごと）／浸水位は消防の目視で厳密比較しない／西舞鶴冠水箇所は原因帰属しない。`scripts/13` は `datum=2011` 取得で現行の地形世代と整合、**コード変更なし**。測地成果2024 への移行は地形データ更新と揃えて判断。 |

**FARR（Flow Accumulation Realtime Renderer, mite-shiru 社 https://mite-shiru.co.jp/farr/ ）の
ロジックを取り込む** **[実測: 外部プロダクト調査]**（2026-09-01）。
FARR は DEM だけで「一様降雨時に地表流がどこに集まるか」をブラウザでリアルタイム描画する
汎用ツール（Priority-Flood で窪地充填 → D-infinity で flow accumulation、クリックで集水域抽出）。
**iwagaki が明示的に「やらない」内水・地表流の集中を扱う**ので補完的。取り込む価値はあると判断（UI 表現は別途検討）。

| 効くか | 内容 |
|---|---|
| **新規性なし** | Priority-Flood の minimax は `h_conn(c) = min-max path` として海 seed から計算済み（`docs/design.md`）。同じ量 |
| **これが追加分** | **潮位に依存しない flow accumulation**。「水みち」ラスタ。iwagaki に無い（第 1 段 D8 → 第 4 段で D-infinity。ともに依存追加なし） |
| 格上げ | 現状の「窪地」は *標高<潮位 かつ 海に非連結* の判定だけ。Priority-Flood の副産物（充填深・越流点標高・容積）で原理的な窪地マップにできる → **入れた** |
| UI プリミティブ | クリックで集水域抽出（既存の地物インスペクタ・断面に接続） |
| 31/32 との相乗 | `scripts/32` の仮想吐口を「大きい窪地から順」ではなく**越流点＋排水ネットワーク上の位置**で置ける → **入れた**（2026-09-01） |

**第 1 段（解析ラスタの書き出し）は入れた** **[実測]**（2026-09-01）。
`scripts/33_flow_accum.py` / `src/iwagaki/flow.py` が 4 条件
（baseline / control / highres / pointcloud、pointcloud が無ければ黙って飛ばす）を
`scripts/30` と**同一グリッド**で回し、`data/out/<範囲>/` に

- `flow_accum_{cond}.tif`（D8・一様単位降雨・生値 = 集水セル数。「水みち」）
- `fill_depth_{cond}.tif` / `spill_elev_{cond}.tif` / `pit_id_{cond}.tif`
  （Priority-Flood + ε の副産物。潮位非依存の原理版の窪地）
- `flow_accum_summary.json` / `flow_accum_pits_{cond}.json`（窪地数・面積・容積・
  端で切れた集水の割合・手法・生成時刻）

を出す。確認用の図は `docs/images/` に 3 枚（4 条件の水みち log／highres の窪地マップ／
control↔highres の差分）。手法・OSS 比較は `docs/data.md` §7、結果は `docs/results.md`
「地表流の集中と窪地構造」。
**浸水判定（`h_conn`）には混ぜず、別レイヤに留めた**（`docs/design.md`「やらないこと」）。
FARR のリアルタイム計算エンジンは移植せず、固定 AOI なので事前焼きにした。

**第 2 段（Web タイル化＋viewer 表示）も入れた** **[実測]**（2026-09-01）。
`docs/web_design.md`「水みちと窪地をどう出したか」、`docs/results.md`
「地表流の集中と窪地構造」→「viewer に出した」。

- **「地形の色」に 水みち を追加**（`flood` / `地盤高` の隣。`domain/types.ts` の
  `TerrainPaint`）。`scripts/80` が条件ごとに RGBA タイル（R = log スケールの集水 8bit、
  B = 充填深コード。吉原 z17・広い 2 範囲 z16）。3 範囲すべて。`drainage` は
  `highres` にフォールバック。
- **窪地の越流点マーカー**（「重ねる」の別レイヤ）。`scripts/33` が
  `flow_accum_pits_<条件>.geojson`（面積上位 60 件）、`scripts/83` が `catalog.flow.pits`。
  既存の「窪地（逆流で…）」レイヤ（潮位依存）とは別物として並記。
- 潮位スライダを動かしても水みちの色は変わらない（潮位非依存）。

**第 3 段（AOI 端の集水の過小評価を collar で直す）も入れた** **[実測]**（2026-09-01）。
`src/iwagaki/flow.py` の `route_with_collar` が AOI 外周へ **GSI 5m 標高タイル**
（DEM5A、穴は DEM10B。`src/iwagaki/gsi_dem.py` が `.txt` で取得。`requirements.txt`
は増やさない）の collar 帯を `FLOW_COLLAR_M`（既定 150 m）張ってから Priority-Flood +
D8 を回し、AOI 矩形に clip する。**collar はルーティング専用**で、窪地の充填深・
越流点・容積は AOI 単独の ε 充填面で解いて AOI セルだけ集計する（数値は不変）。
`edge_truncated_fraction` は吉原 highres で 60.2 % → **9.3 %**、baseline で
45.2 % → **29.3 %**。collar が取れなければ collar 無しに落ちる（`collar_used=false`）。
手法・前後比較は `docs/data.md` §7、結果は `docs/results.md`。

**第 4 段（ルーティングを D-infinity にする）も入れた** **[実測]**（2026-09-01）。
`src/iwagaki/flow.py` の `dinf_flow_direction` / `dinf_accumulation` が Tarboton (1997)
の D-infinity（8 三角 facet の最急勾配方向を、それを挟む隣接 2 セルへ角度で按分。
1 セル最大 2 receiver）を **numba も C++ も無しの純 numpy/scipy** で実装した
（`pyflwdir` / `pysheds` が `numba` を要るのは速度のためで、アルゴリズムは配列演算に
落ちる。`docs/data.md` §7「D-infinity も numba / C++ 無しで書けた」）。位相ソートは
既存の Kahn を receiver 2 つに一般化。`route_with_collar` と `scripts/33` に
`method={"dinf","d8"}` を通し、既定 `dinf`・`IWAGAKI_FLOW_METHOD` で上書き可
（`FLOW_METHOD`）。**D8 API は比較用に残した。** D8 → D-inf で尾根の分岐が滑らかに
なり、発散地形（斜面上部・扇状地）の集水が扇形に広がる。谷筋の本流位置はほぼ不変。
数値は `docs/results.md`「地表流の集中と窪地構造」、手法は `docs/data.md` §7。

**第 5 段（クリックで集水域抽出）も入れた** **[実測]**（2026-09-01）。
水みちモードで地図をクリックすると、その地点の上流（集水域）を面で出し、
インスペクタに面積・最大集水・流出先を出す。**案 B（事前分割した流域ポリゴンの
配信）**を採った — receiver グリッドを 0.5m 全域で送るのは非現実的、サーバ往復は
`docs/web_design.md`「水位を動かしてもサーバ往復しない」と相反。`src/iwagaki/flow.py`
の `flow_basins` が主 receiver の流下木を本流の合流点で切ってリーフ流域にし
（小さいものは隣へ併合、上限 `FLOW_BASIN_MAX_COUNT`）、各リーフに `downstream_basin_id`
を持たせる。`scripts/33` が `flow_basins_<条件>.geojson`、`scripts/83` が
`catalog.flow.basins`（highres 1 本、吉原 ~170 個・168 kB）。viewer は
`domain/flow.ts` の純関数（`basinAt` / `catchmentOf`）でクリック点のリーフ＋上流の
全リーフを union し、`three/catchmentLayer.ts` が薄い面＋縁で出す。潮位非依存の
別オーバーレイで `h_conn`・浸水判定には混ぜない。手法は `docs/data.md` §7
「部分流域への分割」、画面は `docs/web_design.md`「クリックで集水域を抽出する」、
数値は `docs/results.md`。

**これで FARR ブロックは残タスクなし。** 断面ツールへの主流路接続（流域の主流路に
沿って測線を自動で引く）だけ**別 PR に切り出し**た — 主流路ポリライン抽出が
`scripts/33` の追加作業になるため。

- ~~**クリックで集水域抽出。**~~ → **入れた**（2026-09-01、第 5 段。上記）。
- ~~**`scripts/32` の仮想吐口を越流点ベースに置く。**~~ → **入れた**（2026-09-01）。
  `scripts/33` の窪地（面積 1000 m² 以上）の越流点セルを陸側端にして `spill_elev`
  の低い順に最大 12 組（東舞鶴 12・西舞鶴 9）。東舞鶴の S2 は既往最高潮位で
  24.85 → 20.46 ha、「吐口がある」仮定に依存する 2 段が 8.68 → 4.29 ha に減った。
  西舞鶴は S2 が S1 とほぼ同一（`docs/results.md`「越流点ベースで『吐口があれば』の
  2 段が縮む」、`docs/flood_simulation_spec.md` §5.2）。`run_all.sh` は
  33 → 32 → 31 の順に変更

**ボーリング／地下可視化は調査止まり。** 上田氏本人が今年度「地層再現は断念、
柱状図単体の縦表示まで」（2026-08-29）。CITY GAP 側も「補間しない・観測点だけ」の
方針で相性は良いが、いまは中の調査に留める。データ源: 国土地盤情報センター
https://ngic.or.jp/ （近年分は XML）。入口候補は既存の「都市断面」への接続。

## 中

| # | 内容 | 根拠 |
|---|---|---|
| 1 | **高潮浸水想定区域図との突き合わせ** | **舞鶴は高潮浸水想定区域に指定されている**（令和 8 年 2 月、京都府。`docs/data.md`）。公表は PDF 7 本 / 計 19 面、すべてラスタで GIS データの配布は無い。**図郭を判読した** — 東舞鶴 AOI は図番号 4〜5。西舞鶴・吉原 AOI も図番号 5 の図郭に入る見込みだが、レンダリング解像度では南西部の浸水が読めず**現物で要確認**（`docs/data.md`「図郭の対応」）。**京都府側は堤防決壊・越波・河川流量を含む動的計算**（陸域 10m 格子、想定台風 910hPa、舞鶴市の浸水面積 852.02 ha）なので、静水位モデルのこちらと数値の一致を期待する比較にはならない。位置づけの確認に留めるのが妥当 |
| 2 | **設計高潮位 / 計画高潮位の個別海岸の数値** [未確認] | 丹後沿岸海岸保全基本計画に算定式（`計画天端高 = 計画高潮位 + うちあげ高 + 余裕高`）と「防護水準は過去に記録されている高潮に基づく計画高潮位」という定義はあるが、**舞鶴の個別海岸の数値そのものが無い**。定義上は既往最高潮位 T.P.+0.93 m が基礎のはずだが、同一視する根拠が文書に無いので **[仮説] 止まり**。推測で埋めない。<br>**高潮浸水想定の基準潮位 T.P.+0.69 m の内訳は判明した**（朔望平均満潮位 T.P.+0.545 m ＋ 異常潮位 0.142 m。`docs/data.md`）。これは確率的な満潮位＋偏差で、**既往最高潮位 0.93 m とも設計高潮位とも別物**。README の「高潮想定の基準潮位 0.69 m」はこの値。**設計高潮位そのものは依然未確認** |
| 3 | **排水路の吐口の位置と敷高** [未確認] | **水門・樋門は入江に無いことが確認できた**（市への照会、2026-08）。連結性モデルの「最大の弱点」は閉鎖構造ではなく**管路**に移った。吐口は**現在把握されておらず市が来年度に調査委託を出す予定**で、**フラップゲートが無い**ので潮位が上がれば逆流しうる。地表面の連結性しか見ていない本モデルは**内陸側を過小評価している**。<br>**「可能性がある」ではなくなった**（2026-08-25、東舞鶴の指摘）。市街は護岸天端 1.0〜1.3 m に囲まれた 0.8〜1.0 m の窪地で、**潮位 0.45 → 0.95 m の 0.5 m ぶんで市街に増えるのは 1.37 ha だけ**（次の 0.3 m で 14.23 ha）[実測]。潮位 0.93 m で連結 15.75 ha に対し窪地が **10.57 ha / 321 地物**。**viewer に「窪地」の印を足した**（配信物は増えない。`docs/web_design.md`）。**判定には混ぜていない**ので、吐口の敷高が分かれば「どの吐口からどこまで」に置き換えられる |
| 4 | **被害調査の位置データと「浸水位」の定義を市に依頼する** | **資料は 2 回分受領済み**（2026-08。`docs/results.md`「実際の被害記録との突き合わせ」）。潮位 0.68 / 0.67 m で床下 39 / 19 戸、**1 cm 差で倍違う**という `h_conn` が最も効く領域のデータだった。**面としての重ね合わせは済んだ**（2026-08-29。`docs/results.md`「面としてなら重ねられた」）。ただし**地物単位の突き合わせが止まっている** — 資料の図面は Google Maps のスクリーンショットで座標が無く、PLATEAU 側にも住所属性が無い（`bldg:address` が 0 件）。依頼するのは (1) 図面の青塗り部分の位置データ (2) **「浸水位」の定義**（地盤面からの水深か床面からの高さか。モデルの浸水深が実測より深く出ている件の解釈が変わる） |

---

## 低

| # | 内容 | 根拠 |
|---|---|---|
| 5 | **`Points` の先の custom point-cloud renderer** | `docs/web_design.md`「レンダラを差し替える条件」に届いていない。deck.gl `PointCloudLayer` からの差し替えは済んでおり、いま `Points` で困っている兆候は無い |

---

## やると決めていないもの（判断が要る）

- **時系列（潮位の時間変化）。** 毎時潮位の入力は作った（`scripts/86`、
  catalog の `water_level.tide_series`）。ただし**時間発展ではない**。
  静水位モデルなので、出すなら「各時刻を別々の水位で解いたパラメータ掃引」
  であることを画面で明示する必要がある。1998 年の既往最高は観測時系列で再現できる。
- **「0.5m とは何のデータか」を画面に戻すか。** 説明文を全部外したあと、
  外部から**まさにその質問**が来た（2026-08）。いまは select のツールチップだけ。
  凡例あたりに一行だけ戻す案がある。
- **点群を出しているとき PLATEAU 建物をどう扱うか。** 箱が点群を覆うので、
  両方 ON で寄ると「建物の形が見えない」。半透明 / 稜線だけ / 自動 OFF / 何もしない。
- **`diff_res`（解像度だけ）を `判定差` ボタンに出すか。** 焼いてあるが載せていない。
  `highres` に割り当てると「0.5m の判定差」の意味が合計 12.7 % から解像度だけの
  3.4 % に変わり、`README.md` と `docs/results.md` の見出しと食い違う。
  **発表でどちらを主役にするか**で決まる。
- **点群のノイズ点。** SLAM は空に散る点を残していて、実測 RGB で塗ると
  水色の粒として目立つ（彩度を 1.15 に抑えて緩和はしてある）。
  高さでクリップするか、配信前の間引きで落とすかは**データ側の判断**。
- **`fast4g` で建物 2 枚と点 56,000 点のどちらを採るか。** 点群の予算を
  1 MB から 0.5 MB に下げると 12 秒窓の建物が 20/22 -> 22/22 になるが、
  常駐点数が 68,622 -> 12,612 に落ちる（`docs/web_results.md`）。
  いまは点群側を採っている（残り 2 枚は 12 秒直後に届く）。**用途で決まる話**なので、
  庁内での使われ方を確認してから決める。
